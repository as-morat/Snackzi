import 'package:flutter/material.dart';

class HomeCatListDetailsWidget extends StatelessWidget {
  const HomeCatListDetailsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      // body:;
    );
  }
}
